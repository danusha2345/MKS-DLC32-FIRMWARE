#pragma once
/*
  SDJobPolicy.h - политика совмещения SD-печати и команд с USB/сети.

  Пока с SD-карты исполняется задание, нельзя пускать в планировщик второй поток
  G-code (с USB/Telnet/Web) — траектории смешаются. Но read-only запросы состояния
  (отчёты GRBL) выполнять во время задания безопасно, что позволяет хосту
  (LightBurn/UGS/WebUI) опрашивать контроллер прямо во время гравировки.

  Функция чистая (без зависимостей от Arduino/ESP32) — покрыта нативными тестами,
  см. Firmware/test/test_sd_job_policy/.
*/

// true  -> строку безопасно выполнить во время активного SD-задания
//          (пустая строка или read-only запрос GRBL: $$ $# $G $I $N $).
// false -> строку нужно отклонить с error: AnotherInterfaceBusy
//          (любой G/M-код, перемещение, $J= jog, $H homing, запись настроек $x=...).
bool line_safe_during_sd_job(const char* line);
